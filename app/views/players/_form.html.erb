<%= form_for(@player) do |f| %>
  <% if @player.errors.any? %>
    <div id="error" class="ui-bar-b ui-bar ui-corner-all">
      <p><%= pluralize(@player.errors.count, "error") %> prohibited this player from being saved:</p>

      <ul>
      <% @player.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="ui-body">
    <%= f.label :name %>
    <%= f.text_field :name, "data-clear-btn" => "true" %>
  </div>
  <div class="ui-body">
    <%= f.label :email %>
    <%= f.text_field :email, "type" => "email", "data-clear-btn" => "true" %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
