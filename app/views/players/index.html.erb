<% title 'Gêmkeeper' %>

<div data-role="content">
  <p id="notice" class="ui-bar-a ui-bar ui-corner-all" ><%= notice %></p>

  <ul data-role="listview" data-inset="true" data-filter="true" data-filter-placeholder="Search friends..." >
    <% @players.each do |player| %>
      <li data-theme="c">
      <a href="<%= player_path(player) %>" data-transition="slide">
        <h2><%= player.name %></h2>
      </a>
      </li>
    <% end %>
  </ul>
</div>

